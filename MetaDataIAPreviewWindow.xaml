<UserControl x:Class="MetaDataIAPlugin.MetaDataIAPreviewWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="920">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Descripcion generada" Grid.Column="0">
                <TextBox Text="{Binding Description}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         BorderThickness="0"/>
            </GroupBox>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <GroupBox Header="Generos" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Genres}" TextWrapping="Wrap" Margin="8"/>
                    </GroupBox>
                    <GroupBox Header="Etiquetas" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Tags}" TextWrapping="Wrap" Margin="8"/>
                    </GroupBox>
                    <GroupBox Header="Caracteristicas" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Features}" TextWrapping="Wrap" Margin="8"/>
                    </GroupBox>
                    <GroupBox Header="Detalle IA" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Detail}" TextWrapping="Wrap" Margin="8"/>
                    </GroupBox>
                    <GroupBox Header="Companias" Margin="0,0,0,8">
                        <StackPanel Margin="8">
                            <TextBlock Text="Desarrollador" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Developers}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <TextBlock Text="Editor" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Publishers}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="Edad, region y categorias">
                        <StackPanel Margin="8">
                            <TextBlock Text="Calificacion por edad" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding AgeRatings}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <TextBlock Text="Region" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Regions}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <TextBlock Text="Categorias" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Categories}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" MinWidth="90" Margin="0,0,8,0" Click="Cancel_OnClick"/>
            <Button Content="Aplicar" MinWidth="90" Click="Apply_OnClick"/>
        </StackPanel>
    </Grid>
</UserControl>
